source = $HOME/.config/hypr/mocha.conf

# Define colors and fonts
$accent = $mauve
$accentAlpha = $mauveAlpha
$font = JetBrainsMono Nerd Font

# GENERAL SETTINGS
general {
    disable_loading_bar = true
    hide_cursor = true
}

# BACKGROUND SETTINGS
background {
    monitor = 
    path = /home/navee/Pictures/img/lockscreen.jpg
    blur_passes = 0.5
    color = $base
}

# TIME LABEL
label {
    monitor =
    text = cmd[update:30000] echo "$(date +"%R")"
    color = $text
    font_size = 90
    font_family = $font
    position = -30, 0
    halign = right
    valign = top
    background_color = $surface0Alpha  # Semi-transparent background
    background_border_radius = 10     # Rounded corners for background
    padding = 15, 10                  # Padding around text
}

# DATE LABEL
label {
    monitor =
    text = cmd[update:43200000] echo "$(date +"%A, %d %B %Y")"
    color = $text
    font_size = 25
    font_family = $font
    position = -30, -150
    halign = right
    valign = top
    background_color = $surface0Alpha  # Semi-transparent background
    background_border_radius = 10     # Rounded corners for background
    padding = 15, 10                  # Padding around text
}

# USER AVATAR (Optional - Uncomment if needed)
# image {
#     monitor = 
#     path = ~/.face.png
#     size = 100
#     border_color = $accent
#     position = 0, 75
#     halign = center
#     valign = center
# }

# INPUT FIELD
input-field {
    monitor =
    size = 300, 60
    outline_thickness = 3
    dots_size = 0.2
    dots_spacing = 0.2
    dots_center = true
    outer_color = $sapphire
    inner_color = $surface0
    font_color = $text
    fade_on_empty = false
    placeholder_text = <span foreground="##$subtext1Alpha">Vanakam Pa! naantha <span foreground="##$textAlpha"><b>naveenuu</b></span></span>
    hide_input = false
    check_color = $skyAlpha
    fail_color = $red
    fail_text = Correcta podura 🙄 <b>($ATTEMPTS)</b>
    capslock_color = $yellow
    position = 0, -300
    halign = center
    valign = center
}

# FINGERPRINT FAILURE MESSAGE
label {
    monitor = 
    text = Fingerprint not accepted. Try again! 😔
    color = $red
    font_size = 20
    font_family = $font
    position = 0, -250
    halign = center
    valign = center
    background_color = $surface0Alpha  # Semi-transparent background
    background_border_radius = 10     # Rounded corners for background
    padding = 10, 5                   # Padding around text
}
