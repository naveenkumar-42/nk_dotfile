[general]
bars = 10  # Adjust number of bars for Waybar
framerate = 60

[input]
method = pulse  # Use PulseAudio or PipeWire
source = auto   # Auto-detect the audio source

[output]
method = raw
raw_target = stdout
data_format = ascii
ascii_max_range = 5  # Lower value for better integration
